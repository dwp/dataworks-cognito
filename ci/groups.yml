groups:
  - name: master
    jobs:
      - management-dev
      - management

  - name: pull-request
    jobs:
      - dataworks-cognito-pr

  - name: user-administration
    jobs:
      - management-user-admin
      - management-dev-user-admin

  - name: update-pipeline
    jobs:
      - update-pipeline
